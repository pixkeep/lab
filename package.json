{
  "name": "@pixkeep/lab",
  "version": "0.1.0",
  "description": "PixKeep Lab — public, reproducible in-browser image-codec experiments. First experiment: canvas toBlob encode matrix across browsers.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev --port 4322",
    "build": "astro build",
    "preview": "astro preview --port 4322",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@jsquash/avif": "^2.1.1",
    "@jsquash/webp": "^1.5.0",
    "@wasm-codecs/mozjpeg": "^1.0.1",
    "astro": "^5.1.0"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0",
    "typescript": "^5.9.3"
  }
}
