import { defineConfig } from 'astro/config';

// PixKeep Lab — static site, deployed on Cloudflare Pages at lab.pixkeep.app.
export default defineConfig({
  site: 'https://lab.pixkeep.app',
  output: 'static',
});
